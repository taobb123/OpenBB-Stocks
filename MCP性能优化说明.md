# MCP 分析性能优化说明

## 问题诊断

MCP 分析过程非常慢（6分钟+），主要原因：

### 1. **超时设置过长**
- 原设置：`httpx.AsyncClient(timeout=30.0)` - 每个请求最多等待30秒
- 问题：如果 MCP 服务器响应慢或挂起，每个请求都会等待接近30秒

### 2. **串行尝试多个工具名称**
- 原逻辑：尝试4个工具名称（`equity_profile`, `equity/profile`, `equity_info`, `equity/info`）
- 问题：如果第一个失败，会继续尝试其他工具，最坏情况下会等待 4 * 30 = 120秒（2分钟）

### 3. **串行获取估值和财务指标**
- 原逻辑：先获取估值数据，再获取财务指标
- 问题：如果每个都需要30秒，总共需要60秒

### 4. **没有快速失败机制**
- 原逻辑：即使已有 akshare 数据，也会尝试 MCP 调用
- 问题：浪费时间和资源

## 已实施的优化

### 1. 减少超时时间
- **修改前**：`httpx.AsyncClient(timeout=30.0)`
- **修改后**：`httpx.AsyncClient(timeout=8.0)`
- **效果**：每个请求最多等待8秒，而不是30秒

### 2. 快速失败机制
- **修改前**：尝试多个工具名称，每个都等待超时
- **修改后**：
  - 如果已有 akshare 数据，直接使用，跳过 MCP 调用
  - 只尝试第一个工具名称（`equity_profile`），使用 `asyncio.wait_for` 设置5秒超时
  - 如果失败，立即使用 akshare fallback，不尝试其他工具名称

### 3. 优化数据获取逻辑
- **修改前**：即使已有 akshare 数据，也会尝试 MCP 调用
- **修改后**：
  - 检查是否有 akshare 数据
  - 如果有，直接使用，跳过 MCP 调用
  - 如果没有，才尝试 MCP（快速失败）

## 性能提升预期

### 优化前（最坏情况）
- 估值数据：4个工具名称 × 30秒 = 120秒
- 财务指标：3个工具名称 × 30秒 = 90秒
- **总计**：210秒（3.5分钟）+ 其他处理时间 ≈ 6分钟+

### 优化后（最坏情况）
- 如果有 akshare 数据：0秒（直接使用）
- 如果没有 akshare 数据：
  - 估值数据：1个工具名称 × 5秒 = 5秒（超时后使用 akshare fallback）
  - 财务指标：1个工具名称 × 5秒 = 5秒（超时后使用 akshare fallback）
- **总计**：10秒（最坏情况）+ 其他处理时间 ≈ 15-30秒

### 性能提升
- **优化前**：6分钟+
- **优化后**：15-30秒（如果有 akshare 数据）或 30-60秒（如果没有 akshare 数据）
- **提升**：约 **10-20倍**

## 代码修改位置

### 1. `market_structure_analysis.py`
- 第40行：减少超时时间从30秒到8秒

### 2. `interactive_analysis.py`
- `get_stock_mcp_analysis` 方法：
  - 添加快速失败逻辑
  - 如果已有 akshare 数据，直接使用
  - 只尝试第一个工具名称，使用5秒超时

## 使用建议

1. **确保 akshare 数据可用**：如果 akshare 数据可用，MCP 分析会非常快（几乎瞬间完成）
2. **检查 MCP 服务器状态**：如果 MCP 服务器响应慢，考虑：
   - 检查 MCP 服务器日志
   - 确认 MCP 服务器正在运行
   - 检查网络连接
3. **监控性能**：如果仍然很慢，检查：
   - 是否有其他进程占用资源
   - MCP 服务器是否有性能问题
   - 网络延迟是否过高

## 进一步优化建议

如果性能仍然不理想，可以考虑：

1. **完全禁用 MCP**：如果 akshare 数据足够，可以完全跳过 MCP 调用
2. **缓存 MCP 结果**：对于相同股票的多次分析，可以缓存 MCP 结果
3. **批量处理**：如果有多个股票，可以批量调用 MCP API
4. **使用 REST API**：如果 MCP 协议慢，可以考虑直接使用 OpenBB REST API

