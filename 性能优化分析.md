# 文件分析功能性能优化分析

## 当前性能问题

### 1. 串行处理（主要瓶颈）
- **问题**：股票逐个分析，没有并行化
- **影响**：如果有N只股票，总时间 = N × 单只股票分析时间
- **示例**：1只股票30秒，10只股票需要5分钟

### 2. 同步API调用阻塞
- **问题**：akshare的API调用都是同步的，会阻塞线程
- **影响**：每个API调用都有网络延迟（通常1-3秒），多个调用累加
- **当前流程**（每只股票）：
  1. 获取历史数据：~2-3秒
  2. 获取基本信息：~1-2秒
  3. 获取实时行情（iTick）：~1-2秒
  4. 获取基本面数据：~2-3秒（内部可能调用多个接口）
  5. 时机分析计算：~0.5秒
  6. 技术指标计算：~0.5秒
  7. MCP分析：~2-5秒（如果可用）
  - **总计**：~10-20秒/只股票

### 3. MCP分析等待时间
- **问题**：MCP工具调用是异步的，但可能超时或失败
- **影响**：如果MCP服务器响应慢，会显著增加分析时间

### 4. 数据获取重复
- **问题**：虽然已优化，但某些情况下仍可能重复获取
- **影响**：增加不必要的网络请求

## 优化方案

### 方案1：并行处理多只股票（推荐，效果最明显）

**实现方式**：使用 `asyncio.gather` 或 `concurrent.futures` 并行处理多只股票

**预期效果**：
- 10只股票：从5分钟降至30-60秒
- 并发数：建议3-5只股票同时分析（避免API限流）

### 方案2：单只股票内部并行化

**实现方式**：对于单只股票，并行获取不同类型的数据
- 历史数据、基本信息、实时行情、基本面数据可以并行获取

**预期效果**：
- 单只股票：从10-20秒降至5-8秒

### 方案3：减少API调用

**优化点**：
1. 缓存基本信息（股票名称、行业等变化不频繁）
2. 如果历史数据已获取，避免重复获取
3. 实时行情可选（如果历史数据足够新）

### 方案4：异步化akshare调用

**实现方式**：将akshare的同步调用包装为异步调用

**预期效果**：
- 可以更好地利用I/O等待时间
- 与其他异步操作更好地配合

### 方案5：优化MCP调用

**优化点**：
1. 设置合理的超时时间（避免长时间等待）
2. 如果MCP失败，快速fallback到akshare数据
3. 批量调用MCP工具（如果支持）

## 推荐实施顺序

1. **第一步**：并行处理多只股票（效果最明显，实现相对简单）
2. **第二步**：单只股票内部并行化（进一步优化）
3. **第三步**：添加缓存机制（减少重复请求）
4. **第四步**：异步化akshare调用（需要更多改动）

## 预期性能提升

| 优化方案 | 单只股票时间 | 10只股票时间 | 提升幅度 |
|---------|------------|------------|---------|
| 当前 | 15秒 | 150秒 | - |
| 方案1（并行3只） | 15秒 | 50秒 | 67% |
| 方案1+2 | 8秒 | 27秒 | 82% |
| 方案1+2+3 | 6秒 | 20秒 | 87% |

## 注意事项

1. **API限流**：并行请求可能触发API限流，需要控制并发数
2. **错误处理**：并行处理时，需要确保单个股票失败不影响其他股票
3. **资源消耗**：并行处理会增加内存和CPU使用
4. **网络稳定性**：并行请求对网络稳定性要求更高

