# 报告生成接口说明文档

本文档列出了 `interactive_analysis.py` 文件中用于生成报告的所有接口及其功能。

## 一、核心报告生成接口

### 1. `format_custom_analysis_report(analysis: Dict[str, Any]) -> str`
**功能**: 格式化自定义股票分析报告的主接口
- **输入**: 分析结果字典（包含股票列表、时机分析、基本面数据等）
- **输出**: 格式化的报告文本字符串
- **功能说明**:
  - 生成报告头部（标题、生成时间）
  - 遍历每只股票，格式化以下内容：
    - 股票基本信息（代码、名称、行业）
    - 买入时机分析（评分、建议、关键因素、技术指标）
    - 价格信息（当前价、52周高低点）
  - 添加策略推荐部分
  - 添加风险提示部分
  - 生成报告尾部

---

## 二、数据获取接口（被报告生成调用）

### 2. `get_stock_timing_analysis(symbol: str) -> Dict[str, Any]`
**功能**: 分析股票买入时机
- **输入**: 股票代码（如 "600519"）
- **输出**: 时机分析结果字典
- **返回数据结构**:
  ```python
  {
      "symbol": "股票代码",
      "current_price": 当前价格,
      "indicators": {
          "MA": {  # 移动平均线
              "MA5": 5日均线,
              "MA20": 20日均线,
              "MA60": 60日均线（可选）,
              "trend": "上升/下降",
              "bullish_arrangement": 是否多头排列,
              "price_above_ma": 价格是否在均线之上
          },
          "RSI": {  # 相对强弱指标
              "value": RSI值,
              "signal": "超买/超卖/强势/弱势/正常"
          },
          "PricePosition": {  # 价格位置
              "high_52w": 52周最高价,
              "low_52w": 52周最低价,
              "current": 当前价格,
              "position_percent": 位置百分比,
              "signal": "极高/高位/中高位/中位/中低位/低位"
          },
          "Breakthrough": {  # 价格突破
              "high_20d": 20日最高价,
              "high_60d": 60日最高价（可选）,
              "breakthrough_20d": 是否突破20日高点,
              "breakthrough_60d": 是否突破60日高点
          },
          "Momentum": {  # 动量指标
              "momentum_5d": 5日动量百分比,
              "momentum_20d": 20日动量百分比,
              "signal": "强势/弱势/正常"
          },
          "Volume": {  # 成交量
              "current": 当前成交量,
              "avg_20d": 20日平均成交量,
              "ratio": 量比,
              "signal": "放量/缩量/正常"
          }
      },
      "timing": {  # 综合时机判断
          "score": 时机评分,
          "recommendation": "买入/观望/谨慎",
          "factors": [关键因素列表]
      }
  }
  ```
- **依赖**: 需要 `self.akshare` 可用
- **调用的底层接口**:
  - `self.akshare.get_stock_historical()` - 获取历史K线数据

### 3. `get_stock_fundamentals_akshare(symbol: str) -> Dict[str, Any]`
**功能**: 使用 akshare 获取股票基本面数据
- **输入**: 股票代码
- **输出**: 基本面数据字典
- **返回数据结构**:
  ```python
  {
      "symbol": "股票代码",
      "info": {股票基本信息},
      "fundamentals": {基本面财务数据},
      "price_data": {
          "current": 当前价格,
          "high_52w": 52周最高价,
          "low_52w": 52周最低价
      }
  }
  ```
- **依赖**: 需要 `self.akshare` 可用
- **调用的底层接口**:
  - `self.akshare.get_stock_info()` - 获取股票基本信息
  - `self.akshare.get_stock_fundamentals()` - 获取基本面财务数据
  - `self.akshare.get_stock_historical()` - 获取历史价格数据

### 4. `analyze_custom_stocks(stock_symbols: List[str], market_type: str = "A股") -> Dict[str, Any]`
**功能**: 分析用户提供的股票列表（异步方法）
- **输入**: 
  - `stock_symbols`: 股票代码列表
  - `market_type`: 市场类型（默认"A股"）
- **输出**: 完整的分析结果字典
- **返回数据结构**:
  ```python
  {
      "timestamp": "时间戳",
      "market_type": "市场类型",
      "stocks": [
          {
              "symbol": "股票代码",
              "timing": {时机分析结果},  # 来自 get_stock_timing_analysis
              "fundamentals": {基本面数据},  # 来自 get_stock_fundamentals_akshare
              "profile": {  # 股票基本信息
                  "info": {详细信息},
                  "name": "股票名称",
                  "industry": "所属行业",
                  "concept": "概念板块"
              }
          },
          ...
      ]
  }
  ```
- **内部调用**:
  - `self.get_stock_timing_analysis()` - 获取时机分析
  - `self.get_stock_fundamentals_akshare()` - 获取基本面数据
  - `self.akshare.get_stock_info()` - 获取股票基本信息

---

## 三、辅助格式化接口

### 5. `_format_strategy_recommendation_for_stocks(stocks: List[Dict[str, Any]]) -> str`
**功能**: 基于股票分析结果生成策略推荐文本
- **输入**: 股票分析结果列表
- **输出**: 策略推荐文本字符串
- **功能说明**:
  - 分析所有股票的时机评分，判断整体市场状态
  - 统计买入/观望/谨慎建议的数量
  - 根据平均评分判断市场状态：
    - 平均评分 >= 3: 趋势市场，推荐趋势策略（MA交叉）
    - 平均评分 <= -1: 震荡市场，推荐反转策略（均值回归）
    - 其他: 混合市场，推荐混合策略
  - 列出所有买入建议股票的代码、评分和关键因素

### 6. `_format_risk_warnings() -> str`
**功能**: 格式化风险提示与触发点
- **输入**: 无
- **输出**: 风险提示文本字符串
- **内容包含**:
  - 5类风险提示（市场波动、行业轮动、个股、策略、流动性）
  - 5个触发点（市场状态改变、行业反转、基本面恶化、技术指标反转、系统性风险）

---

## 四、文件操作接口

### 7. `_generate_report_filename(prefix: str = "custom_stock_analysis") -> str`
**功能**: 生成报告文件名（纯数字日期格式：年月日时分，重复时加序号）
- **输入**: 文件名前缀（实际不使用，保持兼容性）
- **输出**: 文件名字符串（如 "202501211430.txt" 或 "202501211430_1.txt"）
- **命名规则**: YYYYMMDDHHmm 格式，如果文件已存在则添加序号

### 8. `_save_buy_recommendations(stocks: List[Dict[str, Any]]) -> None`
**功能**: 将买入建议的分析统计保存到 Buy.txt 文件（追加模式）
- **输入**: 股票分析结果列表
- **输出**: 无（直接写入文件）
- **功能说明**:
  - 筛选出所有"买入"建议的股票
  - 格式化买入建议信息（包含代码、评分、关键因素）
  - 追加保存到项目根目录的 `Buy.txt` 文件

---

## 五、工作流接口

### 9. `interactive_workflow() -> Optional[Dict[str, Any]]`
**功能**: 交互式工作流主入口（异步方法）
- **输入**: 无（通过用户交互输入）
- **输出**: 分析结果字典或 None
- **功能说明**:
  - 提供5种分析模式选择
  - 调用 `analyze_custom_stocks()` 分析股票
  - 调用 `format_custom_analysis_report()` 生成报告
  - 调用 `_generate_report_filename()` 生成文件名
  - 保存报告到文本文件和JSON文件
  - 调用 `_save_buy_recommendations()` 保存买入建议

---

## 六、底层数据源接口（通过 akshare）

### 10. `self.akshare.get_stock_historical(symbol, start_date, end_date, period, adjust)`
**功能**: 获取股票历史K线数据
- **用途**: 用于计算技术指标（MA、RSI、价格位置、突破、动量、成交量）

### 11. `self.akshare.get_stock_info(symbol)`
**功能**: 获取股票基本信息
- **用途**: 获取股票名称、行业、概念等基本信息

### 12. `self.akshare.get_stock_fundamentals(symbol)`
**功能**: 获取股票基本面财务数据
- **用途**: 获取市盈率、市净率、ROE、毛利率等财务指标

---

## 七、接口调用关系图

```
interactive_workflow()
    ├── analyze_custom_stocks()
    │   ├── get_stock_timing_analysis()
    │   │   └── akshare.get_stock_historical()
    │   ├── get_stock_fundamentals_akshare()
    │   │   ├── akshare.get_stock_info()
    │   │   ├── akshare.get_stock_fundamentals()
    │   │   └── akshare.get_stock_historical()
    │   └── akshare.get_stock_info()
    ├── format_custom_analysis_report()
    │   ├── _format_strategy_recommendation_for_stocks()
    │   └── _format_risk_warnings()
    ├── _generate_report_filename()
    └── _save_buy_recommendations()
```

---

## 八、报告生成流程

1. **数据收集阶段**:
   - `analyze_custom_stocks()` 遍历股票列表
   - 对每只股票调用 `get_stock_timing_analysis()` 和 `get_stock_fundamentals_akshare()`
   - 收集所有分析结果

2. **报告格式化阶段**:
   - `format_custom_analysis_report()` 接收分析结果
   - 格式化每只股票的信息
   - 调用 `_format_strategy_recommendation_for_stocks()` 生成策略推荐
   - 调用 `_format_risk_warnings()` 添加风险提示

3. **文件保存阶段**:
   - `_generate_report_filename()` 生成文件名
   - 保存报告到文本文件
   - 保存JSON数据到文件
   - `_save_buy_recommendations()` 保存买入建议

---

## 九、错误处理

所有接口都包含错误处理机制：
- `get_stock_timing_analysis()`: 返回 `{"error": "错误信息"}` 格式
- `get_stock_fundamentals_akshare()`: 返回 `{"error": "错误信息"}` 格式
- `format_custom_analysis_report()`: 检查错误字段，显示错误提示而不是崩溃

---

## 十、注意事项

1. **依赖要求**: 大部分接口需要 `self.akshare` 可用，否则返回错误信息
2. **异步方法**: `analyze_custom_stocks()` 和 `interactive_workflow()` 是异步方法，需要使用 `await` 调用
3. **数据格式**: 所有接口返回字典格式，便于JSON序列化
4. **文件编码**: 所有文件操作使用 UTF-8 编码

