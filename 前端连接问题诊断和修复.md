# 前端连接问题诊断和修复

## 问题描述

前端控制台显示错误：
- `Failed to load resource: net::ERR_CONNECTION_REFUSED`
- URL: `:8003/api/extract-stock-codes/`
- 导致 `TypeError: Failed to fetch`

## 诊断结果

### 1. 端口状态检查
- ✅ 端口 8003 正在监听（进程 19168）
- ✅ 端口 8000 也在监听（进程 17800）
- ✅ 后端服务器应该正在运行

### 2. 配置检查
- ✅ 前端默认配置：`http://localhost:8003/api`
- ✅ 后端启动脚本：`python manage.py runserver 8003`
- ✅ CORS 配置：已启用，允许所有来源

### 3. 可能的原因

#### 原因 1：后端服务器未正确启动
虽然端口在监听，但 Django 服务器可能没有正确响应请求。

**解决方案**：
1. 检查 Django 服务器日志，确认是否有错误
2. 重新启动 Django 服务器：
   ```bash
   cd market_analysis_api
   python manage.py runserver 8003
   ```

#### 原因 2：URL 格式问题
前端请求的 URL 可能缺少协议或主机名。

**检查**：前端代码中 `API_BASE_URL` 的值应该是完整的 URL：
- ✅ 正确：`http://localhost:8003/api`
- ❌ 错误：`:8003/api/extract-stock-codes/`

#### 原因 3：浏览器缓存或网络问题
浏览器可能缓存了旧的配置或网络连接有问题。

**解决方案**：
1. 清除浏览器缓存
2. 硬刷新页面（Ctrl+Shift+R）
3. 检查浏览器控制台的网络标签，查看实际请求的 URL

#### 原因 4：防火墙或代理问题
Windows 防火墙或代理可能阻止了连接。

**解决方案**：
1. 检查 Windows 防火墙设置
2. 检查是否有代理设置影响本地连接
3. 尝试在浏览器中直接访问 `http://localhost:8003/api/`

## 修复步骤

### 步骤 1：验证后端服务器
在浏览器中直接访问：
```
http://localhost:8003/api/
```

应该看到 JSON 响应：
```json
{
  "status": "ok",
  "message": "Market Analysis API",
  "endpoints": [...]
}
```

### 步骤 2：检查前端配置
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签，找到 `API_BASE_URL` 的值
3. 确认 URL 格式正确

### 步骤 3：测试 API 端点
在浏览器控制台中运行：
```javascript
fetch('http://localhost:8003/api/')
  .then(r => r.json())
  .then(console.log)
  .catch(console.error)
```

### 步骤 4：检查网络请求
1. 打开浏览器开发者工具
2. 切换到 Network 标签
3. 尝试触发文件分析功能
4. 查看失败的请求，检查：
   - 请求 URL 是否正确
   - 请求方法是否正确（POST）
   - 响应状态码是什么

## 快速修复

如果问题仍然存在，尝试以下方法：

### 方法 1：重新启动后端服务器
```bash
# 停止当前服务器（Ctrl+C）
# 然后重新启动
cd market_analysis_api
python manage.py runserver 8003
```

### 方法 2：清除前端缓存
1. 在浏览器中按 F12 打开开发者工具
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

### 方法 3：检查环境变量
确认前端环境变量设置：
```bash
# 在 desktop 目录下
# 检查 .env 文件或环境变量
VITE_API_BASE_URL=http://localhost:8003/api
```

### 方法 4：使用不同的端口
如果 8003 端口有问题，可以尝试使用 8000：
1. 修改 `market_analysis_api/start_server.bat`：
   ```bat
   python manage.py runserver 8000
   ```
2. 修改前端配置 `desktop/src/config/api.ts`：
   ```typescript
   return 'http://localhost:8000/api';
   ```
3. 重新启动服务器和前端

## 调试技巧

### 1. 查看后端日志
Django 服务器控制台会显示所有请求的日志，包括：
- 请求 URL
- 请求方法
- 响应状态码
- 错误信息（如果有）

### 2. 查看前端网络请求
在浏览器开发者工具的 Network 标签中：
- 查看请求的完整 URL
- 查看请求头
- 查看响应内容
- 查看错误信息

### 3. 使用 Postman 或 curl 测试
直接测试后端 API：
```bash
# 测试健康检查
curl http://localhost:8003/api/

# 测试提取股票代码
curl -X POST http://localhost:8003/api/extract-stock-codes/ \
  -H "Content-Type: application/json" \
  -d '{"content": "600519 000001"}'
```

## 常见错误和解决方案

### 错误 1：`ERR_CONNECTION_REFUSED`
**原因**：后端服务器未运行或端口不正确
**解决**：确认服务器正在运行，端口正确

### 错误 2：`CORS policy: No 'Access-Control-Allow-Origin' header`
**原因**：CORS 配置问题
**解决**：检查 `settings.py` 中的 CORS 配置

### 错误 3：`404 Not Found`
**原因**：URL 路径不正确
**解决**：检查 `urls.py` 中的路由配置

### 错误 4：`500 Internal Server Error`
**原因**：后端代码错误
**解决**：查看 Django 服务器日志，找到具体错误

## 预防措施

1. **统一端口配置**：确保前端和后端使用相同的端口
2. **添加健康检查**：前端启动时检查后端是否可用
3. **错误处理**：前端应该有清晰的错误提示
4. **日志记录**：记录所有 API 请求和响应，便于调试

## 下一步

如果问题仍然存在，请提供：
1. Django 服务器控制台的完整日志
2. 浏览器控制台的完整错误信息
3. Network 标签中失败请求的详细信息
4. 前端和后端的版本信息

