# 接口直接使用说明

## 📋 优化内容

### 基本信息获取接口优化

**文件**：`akshare_data_source.py`

**优化前**：
1. 先尝试 `stock_zh_a_spot`（新浪接口）
2. 如果失败，再尝试 `stock_info_a_code_name`（备用）

**问题**：
- `stock_zh_a_spot` 经常失败（解码错误：`Can not decode value starting with character '<'`）
- 导致重复调用，浪费时间
- 用户体验差（看到错误信息）

**优化后**：
- ✅ 直接使用 `stock_info_a_code_name` 接口
- ✅ 移除 `stock_zh_a_spot` 的尝试
- ✅ 简化代码逻辑

## 🔧 代码变化

### 之前
```python
def get_stock_info(self, symbol: str) -> Dict[str, Any]:
    # 1. 先尝试 stock_zh_a_spot
    try:
        spot_df = ak.stock_zh_a_spot()
        # ... 处理数据
        return info_dict
    except Exception as e:
        print(f"⚠️ stock_zh_a_spot 失败: {e}")
    
    # 2. 备用：使用 stock_info_a_code_name
    try:
        info = ak.stock_info_a_code_name()
        # ... 处理数据
        return info_dict
    except Exception as e:
        print(f"⚠️ stock_info_a_code_name 失败: {e}")
    
    return {}
```

### 现在
```python
def get_stock_info(self, symbol: str) -> Dict[str, Any]:
    # 直接使用 stock_info_a_code_name
    try:
        print(f"  使用 stock_info_a_code_name 获取 {symbol} 基本信息...")
        info = ak.stock_info_a_code_name()
        if not info.empty:
            stock_info = info[info["code"] == symbol]
            if not stock_info.empty:
                info_dict = {
                    "股票简称": stock_info.iloc[0].get("name", ""),
                    "名称": stock_info.iloc[0].get("name", "")
                }
                if info_dict.get("股票简称"):
                    print(f"  ✅ 通过 stock_info_a_code_name 获取到基本信息")
                    return info_dict
    except Exception as e:
        print(f"  ⚠️ stock_info_a_code_name 失败: {str(e)[:200]}")
    
    print(f"  ❌ 无法获取 {symbol} 基本信息")
    return {}
```

## 📊 优化效果

### 性能提升
- **接口调用次数**：从 2 次减少到 1 次（如果第一个失败）
- **错误信息**：不再显示 `stock_zh_a_spot` 的错误
- **响应速度**：更快（跳过失败的接口）

### 用户体验
- ✅ 不再看到 `stock_zh_a_spot` 失败的错误信息
- ✅ 直接使用稳定可用的接口
- ✅ 更清晰的日志输出

## ✅ 优化完成

现在 `get_stock_info` 方法：
- ✅ 直接使用 `stock_info_a_code_name` 接口
- ✅ 不再尝试 `stock_zh_a_spot`
- ✅ 代码更简洁、更高效

