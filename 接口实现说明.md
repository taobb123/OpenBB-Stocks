# 接口实现说明

## 📋 接口封装说明

### `get_stock_historical()` 方法

**位置**: `akshare_data_source.py` 中的 `AKShareDataSource` 类

**性质**: 这是一个封装方法，在 akshare 标准接口之上提供统一的调用接口

**内部实现**: 直接调用 akshare 的标准接口，按优先级尝试多个接口

## 🔧 接口调用顺序

### 方法1: `stock_zh_a_hist` (主要接口) ⭐⭐⭐⭐⭐

**特点**:
- 最常用，功能最全
- 支持前复权、后复权、不复权
- 可指定日期范围和周期
- **不需要市场前缀**（如直接使用 "600519"）

**调用示例**:
```python
df = ak.stock_zh_a_hist(
    symbol="600519",
    period="daily",
    start_date="20240101",
    end_date="20241121",
    adjust="qfq"  # 前复权
)
```

**参数说明**:
- `symbol`: 股票代码（6位数字，如 "600519"）
- `period`: 数据周期（"daily", "weekly", "monthly"）
- `start_date`: 开始日期（格式：YYYYMMDD）
- `end_date`: 结束日期（格式：YYYYMMDD）
- `adjust`: 复权类型（"qfq"前复权, "bfq"后复权, ""不复权）

### 方法2-4: 尝试不同复权方式和日期范围

如果方法1失败，会依次尝试：
- 不复权数据（`adjust=""`）
- 后复权数据（`adjust="bfq"`）
- 缩短日期范围（最近6个月）

### 方法5: `stock_zh_a_daily` (备用接口) ⭐⭐⭐⭐

**特点**:
- 接口简洁，同样常用
- 支持复权
- **需要带市场前缀**（如 "sh600519" 或 "sz000001"）

**调用示例**:
```python
# 上海股票
df = ak.stock_zh_a_daily(symbol="sh600519", adjust="qfq")

# 深圳股票
df = ak.stock_zh_a_daily(symbol="sz000001", adjust="qfq")
```

**市场前缀规则**:
- 上海股票（代码以 6 开头）: `sh` + 代码，如 `sh600519`
- 深圳股票（代码以 0 或 3 开头）: `sz` + 代码，如 `sz000001`

**注意**: 
- 如果指定了日期范围，会在获取数据后进行过滤
- 日期列可能是 "date" 或 "日期"，代码会自动识别

## 📊 接口对比

| 接口 | 市场前缀 | 日期范围 | 复权支持 | 稳定性 | 推荐度 |
|------|---------|---------|---------|--------|--------|
| `stock_zh_a_hist` | ❌ 不需要 | ✅ 可指定 | ✅ 支持 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| `stock_zh_a_daily` | ✅ 需要 | ⚠️ 需过滤 | ✅ 支持 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

## 🔄 Fallback 机制

代码实现了完整的 fallback 机制：

```
尝试 stock_zh_a_hist (前复权)
    ↓ 失败
尝试 stock_zh_a_hist (不复权)
    ↓ 失败
尝试 stock_zh_a_hist (后复权)
    ↓ 失败
尝试 stock_zh_a_hist (缩短日期范围)
    ↓ 失败
尝试 stock_zh_a_daily (带市场前缀)
    ↓ 失败
返回空 DataFrame
```

## 💡 使用建议

### 1. 优先使用封装方法

```python
from akshare_data_source import AKShareDataSource

akshare_source = AKShareDataSource()
df = akshare_source.get_stock_historical(
    symbol="600519",
    start_date="20240101",
    end_date="20241121",
    adjust="qfq"
)
```

**优点**:
- 自动尝试多个接口
- 统一的错误处理
- 自动处理市场前缀
- 自动过滤日期范围

### 2. 直接使用标准接口（高级用法）

如果封装方法无法满足需求，可以直接使用 akshare 标准接口：

```python
import akshare as ak

# 方法1: stock_zh_a_hist（推荐）
df = ak.stock_zh_a_hist(
    symbol="600519",
    period="daily",
    start_date="20240101",
    end_date="20241121",
    adjust="qfq"
)

# 方法2: stock_zh_a_daily（备用）
df = ak.stock_zh_a_daily(symbol="sh600519", adjust="qfq")
```

## ⚠️ 常见问题

### Q1: 为什么接口会失败？

**可能原因**:
1. **网络问题**: 无法访问数据源网站（push2his.eastmoney.com）
2. **代理问题**: 代理设置错误或代理服务器不可用
3. **接口变更**: akshare 数据源网站改版，接口暂时失效
4. **参数错误**: 股票代码格式错误或日期格式错误

### Q2: 如何排查问题？

1. **检查网络连接**:
   ```python
   import requests
   try:
       response = requests.get("https://push2his.eastmoney.com", timeout=5)
       print("网络连接正常")
   except:
       print("网络连接失败")
   ```

2. **检查代理设置**:
   ```python
   import os
   print("HTTP_PROXY:", os.environ.get("HTTP_PROXY"))
   print("HTTPS_PROXY:", os.environ.get("HTTPS_PROXY"))
   ```

3. **检查 akshare 版本**:
   ```bash
   pip show akshare
   pip install --upgrade akshare
   ```

4. **运行诊断工具**:
   ```bash
   python check_core_interfaces.py
   ```

### Q3: 如何提高成功率？

1. **更新 akshare**:
   ```bash
   pip install --upgrade akshare
   ```

2. **检查网络环境**: 确保能访问数据源网站

3. **禁用代理**（如果不需要）:
   ```python
   import os
   os.environ.pop("HTTP_PROXY", None)
   os.environ.pop("HTTPS_PROXY", None)
   ```

4. **使用备用接口**: 代码已自动尝试多个接口

## 📝 代码实现细节

### 市场前缀处理

```python
# 自动判断市场前缀
if symbol.startswith("6"):
    daily_symbol = f"sh{symbol}"  # 上海
elif symbol.startswith("0") or symbol.startswith("3"):
    daily_symbol = f"sz{symbol}"  # 深圳
else:
    daily_symbol = symbol
```

### 日期过滤

```python
# 自动识别日期列名
if "date" in df.columns:
    df["date"] = pd.to_datetime(df["date"])
    # 过滤日期范围
elif "日期" in df.columns:
    df["日期"] = pd.to_datetime(df["日期"])
    # 过滤日期范围
```

## 🔗 相关文档

- [akshare 官方文档](https://akshare.readthedocs.io/)
- [核心接口失败诊断报告.md](./核心接口失败诊断报告.md)
- [接口可用性检查报告.md](./接口可用性检查报告.md)

